import iDeviceInfo from './iDeviceInfo';

export default interface iRegisterNewUserData {
  first_name: string;
  university_id: string;
  is_student: boolean;
  dream_name: string;
  cellphone: string;
  phone_code: string;
  pin: string;
  email: string;
}
